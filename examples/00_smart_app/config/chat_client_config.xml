<?xml version="1.0" encoding="UTF-8"?>
<chat_client>
    <!-- Desktop chat client connects to an already-running 00_smart_app agent server. -->
    <server>
        <host>127.0.0.1</host>
        <port>8080</port>
        <connect_timeout_sec>10</connect_timeout_sec>
        <read_timeout_sec>300</read_timeout_sec>
    </server>

    <!-- Client-side model selection list. Do not read server-side app_config.xml. -->
    <models default="deepseek">
        <model>deepseek</model>
        <model>qwen</model>
        <model>kimi</model>
        <model>openai</model>
        <model>claude</model>
    </models>

    <ui>
        <console>false</console>
    </ui>

    <chat>
        <session_id>desktop</session_id>
        <stream>true</stream>
        <temperature>0.7</temperature>
        <top_p>0.95</top_p>
        <top_k>50</top_k>
        <max_tokens>1024</max_tokens>
    </chat>
</chat_client>
